# KDP Unified — Python Dependencies
# Merged from all 28 repositories

# ─── Core ─────────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
pyyaml>=6.0
click>=8.1.0
rich>=13.0.0
typer>=0.9.0

# ─── LLM / AI ─────────────────────────────────────────────────────────────────
anthropic>=0.25.0
openai>=1.30.0
tiktoken>=0.7.0

# ─── Book Formatting (rxpelle/book-formatter base) ────────────────────────────
# Pandoc must be installed separately: https://pandoc.org/installing.html
# XeLaTeX must be installed separately (TeX Live / MiKTeX)
pypandoc>=1.13
python-docx>=1.1.0
ebooklib>=0.18

# ─── Parsing & NLP (leinguyenpei) ─────────────────────────────────────────────
nltk>=3.8
pandas>=2.0.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# ─── Research / Scraping (kdp-scout) ──────────────────────────────────────────
requests>=2.31.0
httpx>=0.27.0
fake-useragent>=1.5.0
backoff>=2.2.1
playwright>=1.44.0    # browser automation (install: playwright install chromium)

# ─── Database (kdp-scout) ─────────────────────────────────────────────────────
# Uses SQLite (stdlib) — no extra dep needed

# ─── Image / Cover Generation ─────────────────────────────────────────────────
Pillow>=10.3.0

# ─── PDF Generation (b7011343/kdp-gpt) ───────────────────────────────────────
reportlab>=4.1.0
weasyprint>=60.0      # HTML → PDF fallback

# ─── Metadata & Validation ────────────────────────────────────────────────────
pydantic>=2.7.0
jsonschema>=4.22.0

# ─── Data Science / Trends (wesleyscholl) ─────────────────────────────────────
matplotlib>=3.8.0
seaborn>=0.13.0
wordcloud>=1.9.0
scipy>=1.13.0

# ─── Publishing / EPUB ────────────────────────────────────────────────────────
# epub normalizer (jonatasolmartins)
fuzzywuzzy>=0.18.0
python-Levenshtein>=0.25.0

# ─── Audiobook / TTS ──────────────────────────────────────────────────────────
elevenlabs>=1.2.0     # optional, for audiobook pipeline

# ─── Dev / Testing ────────────────────────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.0
